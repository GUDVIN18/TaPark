# TA-Park AI support # TaPark

В папке front нужно зайти в файл index.html и на строчку 800
- const API_SECRET = "";
указать API_SECRET из .env DOCKER_SECRET

при пуше можно прописывать временно 
git add .
git restore --staged app/front/
чтобы не добавлять фронт с прописаным API_SECRET